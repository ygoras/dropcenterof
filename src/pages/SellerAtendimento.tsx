import Atendimento from "./Atendimento";

// Reuses the same component — useTickets filters by tenant automatically
const SellerAtendimento = () => <Atendimento />;

export default SellerAtendimento;
